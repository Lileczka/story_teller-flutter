name: story_teller
description: A new Flutter project.
publish_to: "none"
version: 0.1.0

environment:
  sdk: ">=2.19.6 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/
 # example:
  fonts:
  - family: 'Caveat'
    fonts:
      - asset: fonts/Caveat-VariableFont_wght.ttf
        weight: 500
        style: normal


  
  
